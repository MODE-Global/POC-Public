name: Database Sync - Tuesday

on:
  workflow_dispatch:
  schedule:
    - cron: '*/2 * * * *'
     
jobs:
  Job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Job1 Testing
        run: echo "Job1 first"
        
  Job2:
    runs-on: ubuntu-latest
    steps:
      - name: Job2 Testing
        run: echo "Job2 Second"
